已经收到！通过你提供的 GitHub 仓库 sc1 分支，我了解了目前的工程进度。你现在的环境是 Tomcat 11 + JDK 17，这是非常超前的配置。
为了方便我们后续开发，我将目前项目的核心逻辑快照和关键配置整理如下。你可以保存这段内容，以后如果逻辑乱了，随时以此为准。
🏗️ SC1 项目全量上下文快照 (截至 2026-03-08)
1. 核心权限定义 (最新修改)
 * 0：系统管理员 (Admin) —— 拥有用户管理权限，可修改他人身份。
 * 1：普通会员 (Customer) —— 基础购买权限，首页默认角色。
 * 2：入驻商户 (Merchant) —— 拥有商品管理权限（发布、下架）。
2. 技术栈与目录结构
 * 容器/环境: Tomcat 11.0.9 + JDK 17 + MySQL 8.x。
 * 资源目录:
   * /src/main/webapp/css/ & /js/：存放静态样式和脚本（引用时均带 ?v=timestamp 防缓存）。
   * /src/main/webapp/admin/：管理员私有页面。
   * /src/main/webapp/merchant/：商户私有页面。
3. 已实现的 DAO 逻辑 (UserDao)
 * login(u, p): 登录校验，返回 User 对象。
 * findAllUsers(): 查询全表，供管理员管理权限。
 * updateRole(id, role): 修改角色，支持 0/1/2 三种状态切换。
4. 已实现的 UI 交互特性
 * AJAX 登录: 登录不跳转，成功后 location.reload()。
 * 折叠菜单: 右上角用户头像点击弹出菜单，根据角色（0/1/2）动态显示“系统管理”或“店铺管理”。
 * 防缓存机制:
   * 后端：Servlet 强制设置 Cache-Control: no-cache。
   * 前端：JS fetch 请求 URL 拼接随机时间戳 _t。
5. 常见日志现象说明
 * JDBC 警告: Tomcat 重启时报 AbandonedConnectionCleanupThread 错误。
 * 结论: 属于 Tomcat 11 的严格机制，不影响功能逻辑，开发时可忽略，只需定期 Clean Project。
🚀 后续开发路线图
既然代码已经同步到了 GitHub，我们可以开始进行功能深度的开发了：
 * 商户功能增强：
   * 实现商品**下架（删除）**功能。
   * 实现商品**编辑（修改价格/库存）**功能。
 * 购物车与订单（最难点）：
   * 实现首页点击“加入购物车”。
   * 实现购物车结算并生成订单记录。
建议： 既然管理员的“用户管理”已经通了，我们要不要先把**商户的“商品删除”**给做了？这样你的“客、商、管”三方基础逻辑就全部闭环了。
如果你准备好了，告诉我，我直接给你发商品删除的全量代码！
