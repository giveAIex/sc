<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- 项目名称 -->
    <display-name>sc_mall</display-name>

    <!-- 欢迎页 -->
    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- 404错误页 -->
    <error-page>
        <error-code>404</error-code>
        <location>/login.jsp</location>
    </error-page>

    <!-- 500错误页 -->
    <error-page>
        <error-code>500</error-code>
        <location>/login.jsp</location>
    </error-page>

    <!-- 1. 字符编码过滤器（最高优先级，解决中文乱码） -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>ignore</param-name>
            <param-value>false</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

<!-- 2. XSS防护过滤器（先注释，解决启动失败） -->
<!-- 
<filter>
    <filter-name>XSSFilter</filter-name>
    <filter-class>com.sc.filter.XSSFilter</filter-class>
</filter>
<filter-mapping>
    <filter-name>XSSFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>
-->

<!-- 3. 自动登录过滤器（先注释，解决启动失败） -->
<!-- 
<filter>
    <filter-name>AutoLoginFilter</filter-name>
    <filter-class>com.sc.filter.AutoLoginFilter</filter-class>
</filter>
<filter-mapping>
    <filter-name>AutoLoginFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>
-->

<!-- 4. 权限校验过滤器（先注释，解决启动失败） -->
<!-- 
<filter>
    <filter-name>AuthFilter</filter-name>
    <filter-class>com.sc.filter.AuthFilter</filter-class>
</filter>
<filter-mapping>
    <filter-name>AuthFilter</filter-name>
    <url-pattern>/*</url-pattern>
</filter-mapping>
-->

</web-app>
